class Achievment {
    constructor({ id, title, description, icon, reachValue, reachType }) {
        this.id = id;
        this.title = title;
        this.description = description;
        this.icon = icon;
        this.reachValue = reachValue;
        this.reachType = reachType;
    }
}

export default Achievment;